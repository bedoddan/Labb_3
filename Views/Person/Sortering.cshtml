@model Labb_3.Models.FiltreringModell


@{
    ViewData["Title"] = "Sortering";


    int i = 0;
    if (ViewData["ValdArt"] != null) { i = (int)ViewData["ValdArt"]; }
    string s1 = ViewBag.message;
}

<h1>sortering</h1>

<p>
    <a asp-controller="Person" asp-action="InsertFisk">Rapportera Ny Fångst</a>
</p>

<p> @ViewBag.error</p>

<p>Det inskickade värdet är: @ViewData["ValdArt"] </p>

<form asp-action="Sortering" method="post">
    <div class="form-group">
        <input type="submit" id="Sortering" name="sort" value="Sortera på namn" class="btn btn-dark" />
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Art</></th>
            <th>Vikt</th>
            <th>Vatten</th>
            <th>Namn</a></th>
            <th>typ</th>
            <th>färg</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Labb_3.Models.Totalus f1 in Model.TotalusFiltLista)
        {
            <tr>

                <td>@f1.ID</td>
                <td>@f1.Art</td>
                <td>@f1.Vikt</td>
                <td>@f1.Vatten</td>
                <td>@f1.Namn</td>
                <td>@f1.Typ</td>
                <td>@f1.Color</td>

                <td>
                    @Html.ActionLink("Edit", "UpdateFisk", new { id = f1.ID }) |

                    @Html.ActionLink("Delete", "DeleteFisk", new { id = f1.ID })
                </td>
            </tr>
        }
    </tbody>
</table>
